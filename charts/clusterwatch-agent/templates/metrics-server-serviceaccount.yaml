{{- if .Values.addons.metricsServer.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: clusterwatch-metrics-server
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: clusterwatch-agent
    app.kubernetes.io/component: metrics-server
{{- end }}
