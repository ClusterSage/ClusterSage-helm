ClusterSage platform installed.

Verify:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl logs deploy/clustersage-backend -n {{ .Release.Namespace }}
  kubectl logs deploy/clustersage-frontend -n {{ .Release.Namespace }}
