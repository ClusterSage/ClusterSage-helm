apiVersion: v1
kind: ConfigMap
metadata:
  name: clustersage-config
  namespace: {{ .Release.Namespace }}
data:
{{- range $k, $v := .Values.backend.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
  NEXT_PUBLIC_API_URL: {{ .Values.frontend.env.NEXT_PUBLIC_API_URL | quote }}
