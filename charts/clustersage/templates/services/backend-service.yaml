apiVersion: v1
kind: Service
metadata:
  name: clustersage-backend
  namespace: {{ .Release.Namespace }}
spec:
  selector: { app: clustersage-backend }
  ports:
    - name: http
      port: 80
      targetPort: http
